{
  minify: { domain: 'Logical', required: true },
  cacheSize: { domain: 'DataSize', required: true },
  cacheMaxFileSize: { domain: 'DataSize', required: true },
  gzip: { domain: 'Logical', required: true },
  preprocess: { domain: 'Nomens' },
  index: { domain: 'Logical', required: true }
}
