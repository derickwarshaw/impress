{
  enabled: { domain: 'Logical', required: true },
  keepDays: { domain: 'Days', required: true },
  writeInterval: { domain: 'Duration', required: true },
  writeBuffer: { domain: 'Count', required: true },
  toFile: { domain: 'Nomens' },
  stdout: { domain: 'Nomens' }
}
